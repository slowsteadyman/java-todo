- ## 😁프리코스 오픈미션 도전기
- 터미널 투두 앱을 만들어보자!
	- 지금 내 수준에서 적절한 도전은 무엇일까? 낯선 도구와 고난도 문제 중 어떤 것이 나에게 적합할까. 다른 사람들의 1-3주차 코드를 보니 자바 개발 경험이 꽤나 있는 것으로 보였다. 하지만 나는 자바로 제대로 개발해보는 것이 이번이 처음이었고 남들이 다 얘기하는 MVC 패턴 같은것도 전혀 몰랐다. 주마다 피드백을 받으면서 새로운 것을 배워가고 한창 자바 개발에 익숙해지고 있었다. 그래서 낯선 도구를 사용해서 다시 1-3주차의 과정을 밟기보단 지금까지 배웠던 것을 조금 더 발전시켜 자바를 더 공부하고 싶었다.
	- 생산성에 관심이 많았던 나는 항상 내가 만들고 싶은 투두 앱을 만들고 싶었다. flutter로도 해보고 swift로도 해봤는데 둘다 완성하지 못했다. 언어를 전혀 모르는 상태에서 바이브 코딩만으로 UI와 백엔드를 구성하다보니 버거워지고 어려워져서 중간에 다 그만뒀다. 오픈 미션 기한 안에 투두 앱을 만들어볼 수 있을까? 복잡한 UI를 사용하지 말고 터미널을 사용하고, 내가 익숙해진 언어인 JAVA를 사용한다면 2주 안에 해볼만한 도전이 될 것 같았다.
	- 조금 쉬워 보이는데? 지금까지의 것을 발전시키면서도 한가지 새로운 도전적인 요소를 넣고 싶었다. 딱 떠오른 것이 DB였다. java 자료구조로 만들면 앱을 끄는 순간 todo 목록들이 사라질 테니까 많이 아쉬운 앱이 될 것 같았다. 허접하더라도 실제로 사용할 수 있는 앱을 만들고 싶었다. 그렇게 **db를 사용해 터미널 UI에서 동작하는 java todo 앱**을 만들어보자는 도전이 결정되었다.
- 두번의 todo 앱 개발이 실패했던 주된 이유는 지나치게 높고 정밀한 설계 때문이었다. 처음부터 너무 많은 것을 고려하다보니 당장의 것을 만들기 어려웠던 것이다. 이번엔 기간이 짧기도 하니까 새로운 방법론을 적용하기로 했다. 2주 안에 도달할 지점을 정하지 말고 **그냥 간단한 눈앞의 기능 하나를 목표로 하자. 금방 만들면 또 다음 기능을 만들고, 그렇게 2주 동안 무한한 도전을 반복해서 갈 수 있는 만큼 나아가보자.**
- sqlite? mysql?
	- 어떤 데이터베이스를 써야하지? mysql이 sqlite에 비해 성능이 더 좋은 것 같았다. 대규모 데이터 처리도 가능하고, 동시 접속도 가능하고 확장성이 높다고 했다. sqlite를 쓰는 것이 간편하겠지만 mysql을 쓰는게 나중에 발전시키기 좋지 않을까? 하지만 mysql은 서버를 필요로 했다. 내가 서버를 운영하지 않으면 사람들이 앱을 쓰지 못한다고 생각하니 그건 아닌 것 같았다. 그렇다고 각자의 로컬에서 서버를 돌리는 방법도 생각해봤지만 그렇게 하면 sqlite를 쓰는 것과 다름없다는 것을 알았다. 사용자 입장에서 생각해봤을 때 할일 앱을 통해 다른 사람과 상호작용할 일은 거의 없을 것이며 서버 없이 단일 프로그램과 db 파일 하나로 앱을 사용할 수 있다는 것이 훨씬 더 좋을 것 같았다.
	- sqlite를 연동시키는 것도 작은 도전이었다. 홈페이지에는 엄청 많은 다운로드 파일이 있었고, java라고 이름붙은 건 없었다. 찾아보니 java는 jdbc라는 것을 통해 앱과 db를 연동시킬 수 있었다. 그런데 아무리 dependency에 implemetation을 명시해줘도 빨간줄이 뜨며 인식을 못하는 것이었다. project structure에 들어가서 외부 라이브러리도 적어줘봤지만 여전히 sqlite를 찾지 못하는 것이었다. **한참을 씨름한 끝에 gradle 메뉴에서 refresh를 했더니 그제야 import가 제대로 되었다.**
- '할일 추가하기'에서 사용자 경험이 훌륭하려면?
	- 사용자 입장에서 앱을 사용할 때, 어떤 식으로 입력을 받아야 만족스러울지를 많이 고민했다. 이름, 설명, 날짜 등을 한번에 입력받으면 개발자 입장에서는 편하겠지만 사용자 입장에서는 불편할 것이다. '파싱을 위해 띄어쓰기나 특정한 구분자를 넣어주세요'와 같은 제약이 추가될 것이다. 따라서 각 필드값을 차례대로 하나씩 입력받기로 했다. 이름을 입력받고 제대로 입력됐으면 다음 항목을 입력받는 것이다. 또한 각 필드를 입력받으면서 바로 검증을 진행해 재입력을 받기로 했다. **한번에 다 입력받고 할일 생성에 실패할 시 재입력 받는 것이 개발자 입장에서는 편하겠지만 사용자 입장에서는 올바르게 입력했던 내용들도 처음부터 다시 입력해야 하기 때문이다.**
- while try catch의 구조가 반복되는데? 함수형 인터페이스의 등장
	- 할일 이름을 입력받을 때 다음과 같은 과정을 거쳤다. 값 입력받기 -> 값 검증하기 -> 올바른 값이면 리턴 잘못된 값이면 처음으로 돌아가 반복한다. 이것은 while try ~ catch (illegalArgumentException)의 형태를 띄었는데, 생각해보니 '이름' 뿐만 아니라 '내용', '마감 기한', '분류 카테고리' 등도 다 같은 로직을 가질 것이었다. **그래서 번거롭게 반복문을 계속 작성하지 말고 이것을 readUntilValid라는 함수로 만들고 인자로 입력/검증 함수만 넣어주면 되지 않을까?** 라는 생각을 했다. 그래서 함수를 인자로 받는 함수의 개념이 자바에 있나 찾아봤는데 다행히 함수형 인터페이스라는 것이 존재했다. consumer와 supplier등 적절한 함수형 인터페이스를 활용하니 input, validate 함수만 인자로 넣어주면 알아서 유효한 값이 입력될 때까지 반복하는 것이었다. 또한 타입을 명시하지 않고 T로 지정하니 문자열이 아니라 숫자로 들어오는 입력값들도 검증할 수 있었다. 이때까지만 해도 불필요한 코드의 중복을 피하기 위한 함수를 잘 만들었다고 생각했다.
	- 문제는 탭(카테고리)를 입력받는 것에서 발생했다. 이름과 설명을 입력받는 inputName, inputDescription 함수의 경우 아무 인자 없이 문자열을 리턴하는 supplier<>였는데, 카테고리를 입력받는 inputTabId 함수의 경우 기존의 카테고리(탭) 정보를 담고 있는 tabs가 함께 인자로 전달되어 그 탭을 먼저 출력하고 거기서 탭 번호를 사용자가 입력하는 Function<> 이었다. **따라서 supplier와 function을 받는 readUntilValid와 호환되지 않았다.** 이 인터페이스를 깨지 않기 위해 inputTabId를 supplier로 만들고, inputTabId 함수 안에서 tabs를 제작한 후 출력하는 로직도 생각해봤지만, 입출력을 받는 함수는 입출력만 해야한다는 책임 분리를 생각하니 그럴 수는 없었다.
	- 또 다른 문제로는 탭의 경우 input, validation에 더해 parsing이 추가되었는데 기존의 함수형 인터페이스에 세개의 함수를 전달할 수는 없었다. 최대 두개였기 때문이다. 그래서 validator 안에 parser를 넣어 일관성을 유지할까 생각도 했지만 오히려 그것이 일관성을 해치고 책임 분리를 파괴하는 길이었다. 그때서야 알았다. **내가 만든 입력과 검증을 반복하는 readUntilValid라는 함수는 사실 readUntilValid가 아니라 readUntilValidForName&Description이라는 함수였다는 것을.** 그래서 함수형 인터페이스로 만들었던 함수를 지우고 원래의 while try catch로 돌아오게 되었다. 불필요한 중복을 언제나 없앨 수 있는 것은 아니라는 것을 알게 되었다.
- 검증은 어떻게 어디서 해야하지?
	- 로또 과제를 떠올려보면 로또 생성자에서 validate(numbers)를 통해 로또 번호를 검증했었다. 할일 앱에서도 같은 방식으로 new Todo()를 할 때 입력값 검증을 할까 했지만 생각해보니 내 어플리케이션에서는 틀린 값에 한에서만 재입력을 받아야 했다. 생성자에서 검증을 하면 설명 필드에 유효하지 않은 값이 들어왔음에도 나머지 값들을 전부 입력받은 다음에 검증을 하고 틀린 값을 재입력받는 불필요한 일이 발생한다. 그래서 내 어플리케이션에서는 생성자를 통한 검증이 불가하다는 생각을 했다.
	- 할일 생성 시 Tab ID(탭 카테고리 번호)에 대한 효율적인 검증을 어떻게 해야할지 정말 많이 고민했다. 탭 번호는 숫자가 아닌 경우 재입력, 숫자이지만 없는 탭 번호인 경우 재입력, 빈 값인 경우 기본 탭 번호인 2를 할당해야 했다. validation에서도 parser가 호출되고, 문자열인 탭 번호를 숫자로 바꾸는 데에도 parser가 호출되기 때문에 **불필요한 parser 호출의 중복을 어떻게든 줄이고 싶었다.**
	- 그래서 처음에는 탭 번호를 숫자로 변환하지 않기로 했다. 어차피 db 테이블에서 tab Id는 int로 정의가 되어있었기 때문에 프로그램에서 탭 번호를 잠시 저장하는 데 굳이 숫자형을 사용해야할까? 하지만 sqlite에서 임의로 형변환을 해주고 있다는 사실을 알게되었다. 문자열 값을 쿼리에 넣을 경우 where id = '1' 과 같이 되기 때문에 int인 id와 호환이 되지 않는 것이 맞다. 하지만 sqlite에서 알아서 id가 int이므로 '1'을 1로 변환하여 where id = 1로 해준다는 것을 알았다. 이렇게 되면 나중에 **임의로 형변환을 하는 과정에서 문제가 생길 수 있겠다** 싶어 todo 객체도 db 테이블의 스키마와 같은 타입을 갖도록 설계하는 것이 맞겠다는 생각을 했다.
	- 그렇게 parser를 두번 호출하지 않기 위해 생각한 두번째 방법은 **validator에서 검증 후 파싱하여 값을 반환하는 것이었다.** 어차피 값을 검증한다는 것은 유효한 숫자인지 확인한다는 것이기 때문에 그냥 validator에서 검증이 완료된 값을 숫자로 변환하여 전달하면 더 깔끔하지 않을까? 그렇게 3주차까지는 생각했었고 실제로 입력값을 그렇게 검증/파싱했다. 하지만 다시 생각해보니 오히려 책임 분리가 제대로 이뤄지지 않는다는 생각이 들었다. 코드는 더 줄어들 수 있겠지만 validator에서 검증만 하는게 아니라 변환까지 수행하면 그게 진정한 책임 분리가 맞을까? validator는 함수 역할에 걸맞게 검증만 하고 문제가 있을시 예외를 throw, 문제가 없을 시 무사히 통과하는 void 함수인 것이 올바른 설계이지 않을까? 그렇게 parser의 중복을 허용하더라도 명확하게 read -> validation -> parsing을 수행해 더 직관적이고 분리된 코드를 만들게 되었다.
- SQLInjection의 문제가 있지 않을까?
	- 생각해보니 학교 수업에서 데이터베이스 수업을 들을 때 sql 공격을 배웠던 것이 떠올랐다. 사용자가 입력한 sql값을 통해 db에 공격을 할 수 있다는 것이었는데 내 프로그램에도 그럴 수 있을까? insert into TABLE values + "입력값" 에서 어떻게 sql 공격을 하지? 처음엔 괜찮겠다 싶었는데 생각해보니 쿼리를 마감하는 ';'를 쓰면 "값;쿼리"와 같은 입력값으로 공격을 할 수 있다는 것을 알았다. 그래서 찾아보니 다행히 statement가 아닌 **preparedstatement**라는 것이 있었고, 이 라이브러리는 ;와 상관없이 값을 모두 문자열로 처리하여 ? 안에 넣는 것을 통해 안전한 쿼리를 제공한다는 것을 알았다.
- 쿼리 결과를 어떻게 출력할까?
	- **할일 생성은 new Todo( '이름/내용/마감기한/탭번호' )**를 통해 이루어진다. 할일을 입력받을 때 탭 이름을 일일이 입력하면 사용자가 번거로울까봐 탭 아이디만을 입력받게 했다.
	- 하지만 **할일 조회는 '이름/내용/마감기한/탭이름'**의 형태로 제공하고 싶었다. 조회할 때에는 탭 이름을 보여줘야 사용자가 아 이 todo가 이 탭에 속해있구나라는 것을 알 수 있기 때문이다.
	- join 쿼리를 통해 얻은 resultSet에는 '아이디/이름/내용/마감기한/탭번호/탭이름'이 담겨있었다.
	- 가장 편한 방법은 즉시 결과를 출력하는 것이다. 하지만 그렇게 하면 책임 분리 원칙에 위배되기 때문에 결과를 반환하고 view에서 그것을 처리해야 했다.
	- **그래서 생각한 것은 resultSet을 반환하는 것이었다.** 그리고 view에서 resultSet.getString을 통해 필요한 값을 출력하면 되겠구나. 하지만 resultSet을 반환하는 것은 불가능했다. 왜냐하면 resultSet은 링크드 리스트의 형태를 띄고 있는데 반환하면 커넥션이 닫혀 rs.next()가 안되기 때문이다.
	- **그래서 생각한 것은 Map<Integer, List<String>>을 통해 쿼리 결과를 반환하자는 것이었다.** 하지만 이렇게 불필요하게 맵을 만들어야될까? 더 깔끔하고 직관적인 방법은 없을까?
	- **그냥 todo 객체를 통해 탭 번호만을 반환하고 다시 탭 번호를 통해 tab 테이블에 쿼리를 날려 tabName을 알아낼까?** 불필요하게 왜 쿼리를 두번 날려야하지?
	- **그냥 새로운 객체 view용 todo인 todoView를 만들면 되잖아?** todo는 할일 생성을 위해 db와 상호작용하는 객체라면, todoView는 db의 결과를 저장하고 view와 연결되어 출력에 사용하는 객체로 만들면 되지 않을까? **나중에 알게되었는데 이것은 이미 MVC 방법론에 존재하는 dto(data transform object)라는 것이었다.**
	- 이것이 이번 오픈미션을 하면서 느꼈던 흥미로웠던 부분인데, **프로그램의 복잡도가 증가할수록 자연스럽게 MVC 패턴과 유사한 형태로 내가 코드를 설계하려 한다는 것이었다.** 나는 MVC를 들어본적이 없었고 프리코스에서 코드리뷰를 할 때 많은 사람들이 이 패턴을 사용한다는 것을 알게됐다. 정해진 방법론같은건가? 물론 익혀서 사용하는 것도 좋겠지만 프리코스에서 아직 MVC가 언급되진 않았기 때문에 일단 내 힘으로 부딪혀 보고 프리코스 피드백에 따라서만 내 코드를 피드백하자. 그리고 나중에 그것이 필요한 시기가 오게 되겠지? 라고 생각하며 1~3주차 동안의 피드백을 고려하며 코드를 설계하고 있었다. 그런데 책임분리와 이해하기 쉬운 프로그램 작성에 집중하다보니 자연스럽게 비슷한 형태가 되어가고 있다는 것을 알았다. **내가 내 코드를 더 잘 이해하기 위해, 덜 수정하기 위해, 더 편리하게 코드를 조립하기 위해 고민한 것이었다.** 그제서야 조금씩 알게됐다. 아 이 MVC를 사람들이 이래서 쓰는구나. 더 잘 개발하기 위해 쓸 수밖에 없는 것이구나.
- 생성자가 여러개가 될 수 있다고?
	- 쿼리 결과를 저장하기 위해 새로 만든 객체 todoView는 6개의 필드를 갖고 있었다. 하지만 뷰에 항상 6개의 필드를 전달할 필요는 없는 경우도 있었다. **아 그러면 뷰에 전달하기 위한 필드에 맞는 객체를 전부 만들어야되는 건가? 이름/내용만 전달하고 싶을 땐 어떻게 하지? 이름, 내용만을 필드로 갖는 nameDescpriptionView 클래스를 만들어야하나?** 그렇게 고민하다가 생성자가 여러개가 될 수 있다는 것을 알았다! 생성자 정의만 잘 해주면 모든 필드를 채우지 않고도 todoView를 만들 수 있던 것이었다. 별건 아니지만 작은 문제를 발견하고 고민하고 해결책을 찾아 문제를 해결한 것이 뜻깊은 경험이었다.
- 할일 수정시 사용자가 기존에 입력했던 값을 보여주고 그 상태에서 수정할 수 있게 하려면?
	- 사용자가 오타를 내서 그 부분만 수정하고 싶을 수 있다. 예를 들어 할일 설명에 '아침 7시에 일어나서 아침먹고 운동 다녀와서 9시부터 공부 시작하ㄱ' 라고 해보자. 할일 앱에서 이것을 수정한다고 하면 당연히 저 설명이 그대로 나오고, 'ㄱ'을 지운 후에 '기'로 바꿔 저장하면 된다. html input의 value 처럼 말이다. 내 터미널 투두 앱에서도 이것을 반드시 구현하고 싶었다. 수정 시 처음부터 값을 다시 입력받으면 사용자는 너무나 불편하기 때문이다. 그래서 printf나 readline을 사용해 여러가지를 시도해 봤지만 기존의 값을 띄워주는 것까진 가능한데 지우거나 커서를 옮기는 것이 불가했다. 좋은 방법이 없을까? 열심히 찾아본 끝에 **jLine이라는 라이브러리를 발견했다.** 이것은 자체적으로 터미널에서 쓸 수 있는 고급 기능을 java에서도 쓸 수 있게 해주는 라이브러리였다. 이거구나! **lineReader 함수의 buffer 인자로 값을 넣어주면 buffer 값을 포함해 입력을 읽어들이며 buffer 값을 수정할 수도 있겠구나.**
	- **'unable to create system terminal.. create dump terminal'** 에러가 자꾸만 나타났다. 덤프 터미널이 나오면 커서 이동이나 버퍼, 색상 지정 등의 고급 기능을 사용할 수가 없다. jline dump terminal 등을 검색하며 방방 곡곡을 뒤져봤지만 나오는 것은 에러 메시지가 출력되지 않게 하는 것이었다. dump 옵션을 yes로 지정해서 오류로 덤프 터미널이 열려도 문제가 없는 것처럼 보이게 하는 것이다. 하지만 나에게 필요한 것은 덤프 터미널이 아니라 진짜 터미널이 열리는 것이었다. jLine의 공식 문서를 참조하니 gradlw과 함께 사용할 경우 unable to create system terminal 오류가 발생할 수 있다며 위에서 봤었던 같은 해결책을 추천하는 것이었다. 왜 이 문제가 발생하는거지? gradle은 빌드 과정에서 system.in과 system.out을 리다이렉션 해서 사용하는데, JLine에서는 이 리다이렉션 때문에 애초에 실제 터미널을 감지할 수가 없다는 것이었다. 자바 빌드는 gradle아니면 maven인데, gradle을 안 쓸수도 없고. **gradle로 빌드만 하고 프로그램 실행은 터미널에서 직접 하면 어떨까?** 그렇게 빌드를 통해 생성되는 파일은 jar 파일이고 java -jar을 통해 실행할 수 있다는 것을 알았다. 그리고 의존성에 sqlite가 있기 때문에 그것을 포함해서 실행해야 했다. shadowjar라는 것이 있었다. 쉐도우를 사용하면 내 앱과 의존하고 있는 라이브러리까지 모두 포함하여 하나의 단일한 jar 파일을 만들어주는 것이었다. 그 쉐도우 파일을 실행하니 jLine 터미널이 정상적으로 열렸고, 내가 의도했던 buffer 출력과 수정 기능이 정상적으로 동작했다. **그냥 포기하고 조금 단순하게 만들수도 있었는데 좋은 사용자 경험을 고집하며 끝까지 도전했던 덕분에 보다 멋진 앱 기능을 만들 수 있어서 뿌듯했다.**
- 한글 출력이 왜 밀리지?
	- 이상하게 출력할 때 한글이 포함되면 포함된 만큼 출력이 뒤로 밀렸다. 알아보니 한글은 전각 문자여서 터미널에서 두칸을 차지하는 것이었다. length()를 통해 읽어들일 땐 한 글자를 길이 1로 읽지만, 출력할 때 %5s 등으로 전체 길이를 지정하고 한글을 출력하면 한글이 두칸을 차지해 6칸에 출력되는 것이었다. 자바에 한글 라이브러리가 있나? 없는 것 같다. 어떻게 글자가 한글이라는 것을 알 수 있을까? 모든 문자는 유니코드를 통해 문자에 해당하는 숫자 값으로 변환된다. 한글도 한글에 해당하는 유니코드 값이 있었다! **문자열의 각 글자가 한글인지 확인하고 한글 하나당 실제 길이는 1을 더 계산해주고 출력할 때의 포맷팅 너비는 1을 감소시켜주면 되지 않을까?** 그 결과 한글이 들어간 값들도 모두 정상적으로 출력에 성공했다!
- **db를 통해 validation을 할 수도 있지 않을까?**
	- 탭(카테고리) 수정 기능을 만들면서 '탭 번호 검증'을 todo 생성에서의 '탭 번호 검증' 로직과 비슷하게 가던 도중, 문득 새로운 생각이 들었다. 이 앱에는 db가 있는데, 왜 db로부터 조회한 값을 통해 입력값을 검증하지? 그냥 db에 입력값을 직접 넣으면 되지 않을까?
	- 기존에는 db에서 존재하는 모든 탭을 조회하고 그것을 HashMap<탭 번호, 탭 이름>으로 반환했다. 그리고 탭 번호 입력이 들어오면 hashMap에서 조회하여 존재하는 탭인지 확인했던 것이다.
	- 하지만 입력값이 숫자인지만 확인하고, 만약 숫자라면 'select Tab from tabs WHERE id = 입력숫자'을 직접 하는것이다. 그렇게 하면 코스트도 줄어들고 훨씬 더 쉽게 할 수 있지 않을까? 또한 생각해보니 hashMap을 만들고 hashMap에서 값을 찾아보는 동안 db가 최신화된다면 올바른 검증이라고 할 수 있을까?
	- 그렇게 탭 수정 시 입력받는 탭 번호를 db를 통해 검증하는 것으로 구현했다. 그렇게 어렵진 않았지만 기존의 구조에 의문을 갖고 새로운 방법을 시도해서 문제를 해결했다는 점에서 의미있다고 느꼈다.
- **끊임없는 도전의 연속!**
	- 이번 오픈 미션은 정말이지 끊임없는 도전의 연속이었다. 돌이켜보니 '확실한 목표를 설정하지 말고 매일 새로운 도전을 찾자'고 하길 참 잘한 것 같다. 오픈미션 설명회에서 너무 완벽하게 시작하려고 하지 말고 일단 가볍게 시작하고 시간이 남았으면 새로운 걸 또 해보는 걸 추천한다고 했던 말 덕분이다.
	- db와 연동되면 좋을 것 같은데... -> db를 추가해보자!
	- create가 제일 기본적인 기능이겠지? -> todo create를 만들자!
	- read / update / delete도 구현하면 좋을 것 같은데 -> 구현하자! -> create에서 사용했던 함수랑 비슷하게 함수를 만들어야되는데 이름이 겹치네? -> inputForCreate, inputForUpdate로 구분하자! ...
	- 소감문에 적은 것 말고도 변수와 클래스 이름 변경, 비즈니스 로직 변경, 함수 리팩토링 등 끊임없이 작은 문제들을 계속 마주쳤다. 그것은 애초에 완벽하게 설계하지 않았기에 마주칠 수 있는 작은 문제이자 '도전'이었다. 비슷한 문제(도전)들을 계속 해결하다보니 자연스럽게 같은 문제가 발생하지 않게 조금 더 질좋은 코드를 작성할 수 있었다. 그렇게 하니 또 한 등급 더 높은 새로운 도전이 생겨났고 나는 또 생겨난 도전을 해결하는 것에만 집중했다. 목표 없이 무한히 내달리는 것은 어떻게 보면 고통스러운 일이다. 끝도 모르는 길을 계속 힘들게 걸어가야 하기 때문이다. 하지만 어떻게 보면 즐겁고 성취감 있는 일이기도 하다. 오픈 미션을 시작할 때 2주동안 100개가 넘는 문제를 해결해야 할 것을 알았다면 시작부터 압도되어 섣불리 개발을 시작할 수 없었을 것이다. **오히려 끝을 정하지 않았기에 묵묵히 2주동안 나아갈 수 있었다.** 그저 오늘의 도전을 찾고 해결하며 작은 성취를 느꼈다. 그리고 그 뒤엔 내일 해결할 새로운 문제가 보였고 내일은 그것에 집중했다. 그렇게 조금씩 나아가다보니 나름의 구색을 갖춘 나만의 터미널 투두 앱이 만들어진 것이다.
	- 이번 오픈미션은 오로지 내 힘으로 만들었다는 점에서도 의미가 깊다. 지금까지 실패했던 두번의 투두앱은 플러터랑 스위프트를 잘 모르기도 하고 어렵다보니 ai를 활용해서 만들었었다. 그런데 이번에는 그렇게 하고 싶지 않았다. **ai를 쓰지 않아야만 나에게 적절한 도전이었기 때문이다.** ai한테 '터미널에서 동작하는 todo app'을 만들어달라고 하면 하루만에 뚝딱 만들어낼 것이다. 그걸 알고 있었기 때문에 ai를 통한 개발 도움을 전혀 받지 않았다. 계속 정보를 찾다가 더이상 찾지 못하겠으면 '내가 찾고자 하는 함수나 라이브러리가 존재하는지'나 '한글의 유니코드 범위'와 같이 정보를 얻기 위한 최후의 보루로만 사용했다. 간단한 정보를 찾느라 몇시간이 걸리기도 했지만, 그 과정이 있었기 때문에 더 기억에 오래 남고 의미있는 개발이 되었던 것 같다.
- 남아있는 도전들
	- 소감문을 쓰는 와중에도 남아있는 도전들이 계속 생각난다. 어제 발견한 새로운 도전들이다. 곧 결과물을 제출해야 해서 더이상 수정은 못하고 일단 현재의 상태로 제출하겠지만 아쉬움이 남는다. 시간이 더 있었다면 남아있는 도전들을 해결하고 또 새로운 것들에 도전할 수 있었을텐데. 소감문에나마 아직 해결하지 못한 도전들을 적어놓으려 한다.
	- todo에 completed 속성 추가하기
		- 이 앱에는 '할일 완료하기' 기능이 있다. 완료하기를 하면 카테고리(탭)이 '기존의 탭 이름' -> 'done(완료)'로 변경된다. '완료된 할일 조회하기'를 하면 todo의 탭이 'done'인 것들만 조회해서 보여주는 식이다.
		- 그런데 이렇게 하면 계속 현재 할일 목록을 확인할 때 탭이 'done(완료)'가 아닌 것을 따로 조건을 추가해야 한다. 그리고 '완료된 할일 조회하기' 시에 기존에 어떤 탭에 속해있던 것인지 알 수가 없다. 사용자 입장에서는 특정한 탭에 대해서만 완료한 일을 보고싶을 수 있다. 그런데 현재 앱에서는 이것이 불가능하다. 그냥 애초에 설계할 때 todo 의 필드에 boolean completed를 넣었으면 됐을텐데,, 왜 탭을 '완료'탭으로 변경할 복잡한 생각에 꽂혔는지 아쉬움이 남는다.
	- 할일이 남아있는 탭을 삭제할 시 할일 병합하기
		- 지금 앱에서는 탭이 삭제될 경우 탭에 속한 할일들도 모두 조회가 되지 않는다. 삭제되는 것은 아니다. db 테이블에 on delete cascade가 적용되있진 않기 때문이다. 다만 '할일 조회'시 where todo.id = tab.id를 통해 SELECT 질의를 하기 때문에 탭이 삭제되어 tabid가 존재하지 않는 할일은 아예 조회가 되지 않는 것이다.
		- 이에 대해 할일이 남아있는 탭 삭제에 대한 처리를 구현하고 싶다.
			- 할일이 남아있는데 정말 탭을 삭제할 것인지 묻고,
			- 남아있는 할일은 삭제할 것인지 기본 탭으로 이동할 것인지 묻는다.
		- 이렇게 함으로써 사용자가 실수로 탭을 삭제하더라도 만들어놨던 할일 목록이 사라지는 일을 방지할 수 있을 것이다.
	- 리팩토링: 함수 이름 명확하게 짓기, 지나치게 길어진 함수 분리하기
		- 함수 이름을 지으며 나름 함수의 역할을 분리하고 그에 어울리는 이름을 고심하여 지었다. 하지만 그럼에도 여전히 잘못 지은 함수들이 눈에 보인다. 이를테면 tabIdParer의 경우 정확히는 tabIdParserForTodoTask가 맞다. 왜냐하면 할일 생성/조회/수정/삭제 시 이 parser는 빈 값에 대해 TAB_NOT_SELECTED를 반환하는데, 탭 수정/삭제에서는 빈 값을 잘못된 입력으로 처리하기 때문이다. 그러니까 tabIdParserForTabTask가 따로 필요한 것이다. 두 파서의 역할이 다르다. 다른 것으로는 printSuccessMessage가 있다. 이 함수도 정확히는 printSuccessMessageForTodo가 맞다. 이 함수는 todo 생성/조회/수정/삭제 시 성공 메시지인 'success todo %s'를 출력하는 함수인데, 탭 생성/수정/삭제에서는 'success tab %s'를 출력해야 하기 때문이다. 이렇게 역할보다 범용적인 이름을 가진 함수들의 이름을 다시 지어야 한다.
		- 반면 함수의 역할에 맞게 이름을 짓다보니 비슷한 카테고리지만 한끝 다른 역할을 구분하기 위해 이름이 길어질 수밖에 없었다. readTabIdUntilValidForCreate, readTabIdUntilValidForUpdate, readTodoDescriptionForUpdate, readTodoDeadlineForUpdate등이 그것이다. 당연히 readTabId1, readTabId2 이런식으로 짓는 것보단 낫겠지만, 함수가 점점 늘어나고 세분화되면 함수 이름이 지나치게 길어질 것이다. 그러면 가독성이 떨어지고 함수를 호출하는데에도 어려움이 늘어날 것이다.
	- autoIncrement 없애기
		- INSERT 쿼리를 할 때 primary key를 지정하지 않아도 알아서 자동으로 늘어나며 id를 생성해주기 위해 autoincrement를 테이블 스키마에 넣었었다. 하지만 이것이 없어도 알아서 id는 생성되는 것이었고 autoincrement는 비어있는 todo id가 있어도 계속 지금까지 할당됐던 max 값보다 높은 값을 할당하는 옵션이었다. 이것때문에 할일 1, 2, 3을 완료하고 새로운 할일을 생성하면 다시 id 1,2,3이 할당되어야 하는데 4,5,6이 할당되어 점점 숫자가 커지는 불상사가 발생한다. 그래서 autoIncrement를 없애고 이에 맞게 모델들을 다시 수정하고 싶다.
 - **5주동안 알차게 성장하는 개발 경험을 갖게 해준 우아한테크코스에게 감사의 말씀을 전합니다!**
